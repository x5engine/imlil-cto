const { errorHandler, requestLogger } = require('../src/middleware'); describe('Middleware Tests', () => { describe('Error Handler', () => { it('should format error response correctly', () => { const err = new Error('Test error'); const req = {}; const res = { status: jest.fn().mockReturnThis(), json: jest.fn() }; const next = jest.fn(); errorHandler(err, req, res, next); expect(res.status).toHaveBeenCalledWith(500); expect(res.json).toHaveBeenCalledWith({ error: 'Test error' }); }); }); describe('Request Logger', () => { it('should call next function', () => { const req = { method: 'GET', path: '/' }; const res = {}; const next = jest.fn(); requestLogger(req, res, next); expect(next).toHaveBeenCalled(); }); }); });