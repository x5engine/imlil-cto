const express = require('express'); const { notFoundHandler, errorHandler } = require('./middleware/errorHandlers'); const app = express(); app.use(express.json()); // Add your routes here // Must be after all routes - 404 handler app.use(notFoundHandler); // Error handling middleware must be last app.use(errorHandler); module.exports = app;